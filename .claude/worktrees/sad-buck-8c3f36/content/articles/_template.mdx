{/*
  ============================================================
  MDX記事テンプレート
  使い方：
    1. このファイルを {カテゴリ}-{スラッグ}.mdx にコピー
       例: filaments-abs.mdx / printers-bambu-p1s.mdx
    2. _template.meta.ts も同様にコピーして編集
    3. src/lib/articles.ts にエントリを追加（必須）
    4. src/app/{カテゴリ}/{スラッグ}/page.tsx を作成（thin wrapper）

  スタイル仕様:
    - ダークテーマ: 背景 #0f172a / カード #1e293b / テキスト #f1f5f9
    - アクセントカラー: シアン #06b6d4
    - 本文テキスト: #cbd5e1 / ミュート: #94a3b8
    - グリッドはインラインstyleで記述（Tailwind不使用）

  コンポーネント:
    - <ProductCard productId="xxx" /> → src/data/products.ts のIDを指定
    - <RankingItem rank={1} name="..." description="..." price="..."
        pros={[...]} cons={[...]} badge="..." /> → ランキング記事で使用
  ============================================================
*/}

{/*
  ── S オブジェクト ──────────────────────────────────────────
  各セクションで使い回すスタイル定数。
  MDX内でexportしてもTypeScriptの型は付けられないので純粋なJSオブジェクトで書く。
  ※ TypeScript型アノテーションをここに書くとMDXコンパイルエラーになる
  ────────────────────────────────────────────────────────────
*/}
export const S = {
  card: { backgroundColor: "#1e293b", border: "1px solid #334155", borderRadius: "0.75rem", padding: "1.5rem", marginBottom: "2rem" },
  h2: { fontSize: "1.2rem", fontWeight: "700", color: "#f1f5f9", borderLeft: "4px solid #06b6d4", paddingLeft: "0.75rem", marginBottom: "1.25rem" },
  p: { color: "#cbd5e1", lineHeight: "1.85", marginBottom: "0.875rem" },
  muted: { color: "#94a3b8", fontSize: "0.875rem", lineHeight: "1.7" },
  section: { marginBottom: "2.5rem" },
  disclaimer: { color: "#475569", fontSize: "0.75rem", borderTop: "1px solid #1e293b", paddingTop: "1.25rem", lineHeight: "1.6" },
};

{/* ── イントロカード ─────────────────────────────────────────
  記事冒頭の要約カード。「この記事でわかること」を2〜3文で。
  ────────────────────────────────────────────────────────── */}
<div style={S.card}>
  <p style={S.p}>
    <strong style={{ color: "#f1f5f9" }}>〜〜〜（キーワード）</strong>は〜〜〜です。
    〜〜〜のため、〜〜〜な方に人気があります。
  </p>
  <p style={{ ...S.p, marginBottom: 0 }}>
    この記事では〜〜〜について〜〜〜と〜〜〜を解説します。
    関連する「<a href="/howto/beginners" style={{ color: "#06b6d4", textDecoration: "underline", textUnderlineOffset: "3px" }}>初心者ガイド</a>」もあわせてご覧ください。
  </p>
</div>

{/* ── セクション1：〜とは ─────────────────────────────────
  id は _template.meta.ts の toc[].id と一致させること
  ────────────────────────────────────────────────────────── */}
<section id="intro-section" style={S.section}>
  <h2 style={S.h2}>〜〜〜とは</h2>
  <div style={S.card}>
    <p style={S.p}>
      〜〜〜は<strong style={{ color: "#f1f5f9" }}>〜〜〜</strong>です。
      〜〜〜のため、〜〜〜の場面でよく使われます。
    </p>
    <p style={{ ...S.p, marginBottom: 0 }}>
      〜〜〜という特性があり、〜〜〜な用途に適しています。
    </p>
  </div>
</section>

{/* ── セクション2：メリット・デメリット（2カラムグリッド） ──
  pros/consを左右に並べるレイアウト。フィラメント・機種記事で多用。
  ────────────────────────────────────────────────────────── */}
<section id="pros-cons" style={S.section}>
  <h2 style={S.h2}>メリット・デメリット</h2>
  <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "1rem" }}>
    <div style={{ backgroundColor: "rgba(16,185,129,0.06)", border: "1px solid rgba(16,185,129,0.2)", borderRadius: "0.75rem", padding: "1.25rem" }}>
      <p style={{ color: "#10b981", fontWeight: "700", fontSize: "0.85rem", marginBottom: "0.75rem" }}>✓ メリット</p>
      <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.4rem" }}>
        <li style={{ color: "#cbd5e1", fontSize: "0.8rem" }}>• 〜〜〜</li>
        <li style={{ color: "#cbd5e1", fontSize: "0.8rem" }}>• 〜〜〜</li>
        <li style={{ color: "#cbd5e1", fontSize: "0.8rem" }}>• 〜〜〜</li>
      </ul>
    </div>
    <div style={{ backgroundColor: "rgba(239,68,68,0.06)", border: "1px solid rgba(239,68,68,0.2)", borderRadius: "0.75rem", padding: "1.25rem" }}>
      <p style={{ color: "#ef4444", fontWeight: "700", fontSize: "0.85rem", marginBottom: "0.75rem" }}>✗ デメリット</p>
      <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.4rem" }}>
        <li style={{ color: "#cbd5e1", fontSize: "0.8rem" }}>• 〜〜〜</li>
        <li style={{ color: "#cbd5e1", fontSize: "0.8rem" }}>• 〜〜〜</li>
      </ul>
    </div>
  </div>
</section>

{/* ── セクション3：設定・スペック一覧（縦リスト） ────────────
  印刷設定・スペック表示に使う行リスト。
  ────────────────────────────────────────────────────────── */}
<section id="how-to-use" style={S.section}>
  <h2 style={S.h2}>推奨設定・スペック</h2>
  <div style={{ display: "flex", flexDirection: "column", gap: "0.625rem" }}>
    {[
      { label: "〜〜〜", val: "〜〜〜" },
      { label: "〜〜〜", val: "〜〜〜" },
      { label: "〜〜〜", val: "〜〜〜" },
    ].map((item) => (
      <div key={item.label} style={{ display: "flex", gap: "1rem", backgroundColor: "#1e293b", border: "1px solid #334155", borderRadius: "0.5rem", padding: "0.75rem 1.25rem", alignItems: "center" }}>
        <span style={{ color: "#06b6d4", fontWeight: "700", fontSize: "0.8rem", flexShrink: 0, minWidth: "120px" }}>{item.label}</span>
        <span style={S.muted}>{item.val}</span>
      </div>
    ))}
  </div>
</section>

{/* ── セクション4：おすすめ製品（ProductCard） ───────────────
  productId は src/data/products.ts のキーを指定。
  利用可能なID例:
    bambu-pla-basic / esun-pla-plus / overture-pla
    bambu-a1-mini / bambu-a1 / bambu-p1s / ender3-v3-se
  ────────────────────────────────────────────────────────── */}
<section id="recommend" style={S.section}>
  <h2 style={S.h2}>おすすめ製品</h2>
  {/* ProductCardを商品数分並べる */}
  <ProductCard productId="bambu-pla-basic" />
  <ProductCard productId="esun-pla-plus" />

  {/* CTA ブロック（別記事へ誘導） */}
  <div style={{ backgroundColor: "rgba(6,182,212,0.06)", border: "1px solid rgba(6,182,212,0.2)", borderRadius: "0.75rem", padding: "1.5rem" }}>
    <p style={S.p}>
      詳しい比較・購入リンクは以下のランキング記事をご覧ください。
    </p>
    <a href="/filaments/recommend" style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", backgroundColor: "#06b6d4", color: "#0f172a", fontWeight: "700", fontSize: "0.875rem", padding: "0.625rem 1.25rem", borderRadius: "0.5rem", textDecoration: "none" }}>
      → 詳細ランキングを見る
    </a>
  </div>
</section>

{/* ── セクション5：ランキング形式（RankingItem） ─────────────
  ランキング記事ではこのパターンを使う。
  export const rankings = [...] でデータを先に定義して .map() するのが推奨。
  ────────────────────────────────────────────────────────── */}
{/*
export const rankings = [
  {
    rank: 1,
    name: "製品名",
    asin: "ASIN番号（任意）",
    description: "製品説明文",
    price: "約〜〜円",
    amazonUrl: "https://www.amazon.co.jp/dp/ASIN?tag=22020-22",
    pros: ["長所1", "長所2"],
    cons: ["短所1"],
    badge: "バッジテキスト",
  },
];

<section id="ranking" style={S.section}>
  <h2 style={S.h2}>おすすめランキング</h2>
  <div style={{ display: "flex", flexDirection: "column", gap: "1.25rem" }}>
    {rankings.map((item) => (
      <RankingItem key={item.rank} {...item} />
    ))}
  </div>
</section>
*/}

{/* ── セクション6：比較テーブル ──────────────────────────────
  複数製品・素材を比較するテーブル。overflowX: "auto" でスマホ対応。
  ────────────────────────────────────────────────────────── */}
{/*
<section id="comparison" style={S.section}>
  <h2 style={S.h2}>〜〜〜比較表</h2>
  <div style={{ overflowX: "auto" }}>
    <table style={{ width: "100%", borderCollapse: "collapse", fontSize: "0.8rem" }}>
      <thead>
        <tr style={{ borderBottom: "2px solid #334155" }}>
          {["項目", "A", "B", "C"].map((h) => (
            <th key={h} style={{ color: "#06b6d4", fontWeight: "700", padding: "0.625rem 0.75rem", textAlign: "left" }}>{h}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {[
          ["行1", "値A", "値B", "値C"],
          ["行2", "値A", "値B", "値C"],
        ].map((row, i) => (
          <tr key={i} style={{ borderBottom: "1px solid #1e293b", backgroundColor: i % 2 === 0 ? "#1e293b" : "transparent" }}>
            {row.map((cell, j) => (
              <td key={j} style={{ color: j === 0 ? "#94a3b8" : "#cbd5e1", padding: "0.625rem 0.75rem" }}>{cell}</td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</section>
*/}

{/* ── まとめセクション ────────────────────────────────────────
  ────────────────────────────────────────────────────────── */}
<section id="summary" style={S.section}>
  <h2 style={S.h2}>まとめ</h2>
  <div style={S.card}>
    <p style={S.p}>
      〜〜〜については<strong style={{ color: "#f1f5f9" }}>〜〜〜</strong>が最もおすすめです。
      〜〜〜な場合は<strong style={{ color: "#f1f5f9" }}>〜〜〜</strong>を選ぶと良いでしょう。
    </p>
    <p style={{ ...S.p, marginBottom: 0 }}>
      機種選びは「<a href="/printers/recommend" style={{ color: "#06b6d4", textDecoration: "underline", textUnderlineOffset: "3px" }}>おすすめプリンターランキング</a>」も参考にしてください。
    </p>
  </div>
</section>

{/* ── アフィリエイト免責表示（必須・全記事に入れる） ───────── */}
<p style={S.disclaimer}>
  当サイトはAmazonアソシエイト・楽天アフィリエイト・ValueCommerce・メルカリアンバサダーに参加しています。記事内のリンクから商品を購入すると、売上の一部が当サイトに還元される場合があります。価格は記事執筆時点のものです。
</p>
